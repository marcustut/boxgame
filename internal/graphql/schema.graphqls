# GraphQL schema example
#
# https://gqlgen.com/getting-started/

scalar Time

type User {
  id: ID!
  username: String!
  name: String!
  contact: String
  dob: Time
  createdAt: Time!
  updatedAt: Time!
  teamId: String
}

type Query {
  users: [User!]!
}

# type Mutation {
# }
